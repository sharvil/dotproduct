namespace Labs {
  /** True if trails should follow bullets when they're fired. */
  export const BULLET_TRAILS = true;

  /** True if trails should follow bombs when they're fired. */
  export const BOMB_TRAILS = true;

  /** True if the debug UI should be displayed. */
  export const DEBUG_UI = false;

  /** True if we should display when the next game is taking place. */
  export const NEXT_GAME_VIEW = false;
}

export default Labs;
